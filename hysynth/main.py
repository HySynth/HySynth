import argparse


def main():
    # this ensures that the package can be run directly from the command line
    parser = argparse.ArgumentParser(description='Construct hybrid automata from data.')


if __name__ == "__main__":
    main()
